import { Buttonclick } from './buttonclick';

describe('Buttonclick', () => {
  it('should create an instance', () => {
    const directive = new Buttonclick();
    expect(directive).toBeTruthy();
  });
});
